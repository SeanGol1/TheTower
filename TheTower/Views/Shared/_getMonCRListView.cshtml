@model IEnumerable<TheTower.Models.CRRoll>
    <div class="row">
        @foreach (var item in Model)
        {

        <p>
            @Html.DisplayFor(modelItem => item.RollNumber) - @Html.DisplayFor(modelItem => item.XP)
        </p>

        }
    </div>

